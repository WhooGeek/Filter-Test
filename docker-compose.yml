services:
  playwright:
    build: .
    image: mytest:latest
    container_name: playwright-tests
    working_dir: /app
    volumes:
      - .:/app
    command: ["pytest", "-s", "-q"]
    environment:
      - FG_ID=${FG_ID}
      - FG_PW=${FG_PW}
